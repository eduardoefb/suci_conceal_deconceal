# Core crypto — PyCrates + CryptoMobile are installed by `suci_tool.py --setup-env` or auto-bootstrap.
cryptography>=42.0.0

# Build CryptoMobile from source (Rust extension via setuptools-rust).
setuptools>=69.0.0
wheel>=0.42.0
setuptools-rust>=1.9.0

# Graphical UI (`python suci_tool.py` or `python suci_tool.py --gui`).
customtkinter>=5.2.0
